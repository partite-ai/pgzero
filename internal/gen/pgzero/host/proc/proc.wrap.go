// Code generated by wacogo-witgen from pgzero.wit. DO NOT EDIT.

// The pgzero "kernel": everything a Postgres process needs that WASI does not
// provide. The guest side lives in wasm/libpgzero/src (libpgzero), which implements
// the POSIX functions Postgres calls on top of these imports.
//
// Conventions:
//   - Every function that can fail returns s32: >= 0 on success, or a
//     negated wasi-libc errno value.
//   - Pointer-typed parameters (u32 named *-addr or buf) are addresses in
//     the caller's linear memory. The host reads and writes them directly,
//     using wasi-libc's struct layouts. This keeps hot paths free of
//     canonical-ABI copies.
//   - Functions that block return -EINTR when a signal that is neither
//     blocked nor ignored becomes pending (see sig).
//
// Processes. Each process is a separate instance of the same component.
package proc

import (
	"context"
	"fmt"

	"github.com/partite-ai/wacogo"
	"github.com/partite-ai/wacogo/host"
)

// procWrapper is the concrete type returned by WrapInstance
// as a Proc. Held internally; users hold it via the Proc
// interface.
type procWrapper struct {
	callerInst *host.ComponentInstance
	fnExit     *host.ExportedFunc
	fnGetpid   *host.ExportedFunc
	fnGetppid  *host.ExportedFunc
	fnKill     *host.ExportedFunc
	fnSpawn    *host.ExportedFunc
	fnWaitpid  *host.ExportedFunc
}

// WrapInstance returns a Proc that forwards method calls to
// callee. caller is a Factory-built host instance whose state hosts any
// handles minted by callee during those calls.
func WrapInstance(caller *host.ComponentInstance, callee *wacogo.ComponentInstance) Proc {
	w_ := &procWrapper{callerInst: caller}
	w_.fnExit = host.WrapExportedFunc(callee.ExportedFunc("exit"))
	w_.fnGetpid = host.WrapExportedFunc(callee.ExportedFunc("getpid"))
	w_.fnGetppid = host.WrapExportedFunc(callee.ExportedFunc("getppid"))
	w_.fnKill = host.WrapExportedFunc(callee.ExportedFunc("kill"))
	w_.fnSpawn = host.WrapExportedFunc(callee.ExportedFunc("spawn"))
	w_.fnWaitpid = host.WrapExportedFunc(callee.ExportedFunc("waitpid"))
	return w_
}
func (w *procWrapper) Exit(ctx context.Context, status int32) error {
	arg0_ := status
	{
		callErr_ := w.fnExit.CallRaw(ctx, w.callerInst,
			func(caller, callee *host.CallContext, stack []uint64) {
				stack[0] = uint64(uint32(arg0_))
			},
			func(caller, callee *host.CallContext, stack []uint64) {
			},
		)
		if callErr_ != nil {
			return fmt.Errorf("wacogo/witgen: CallRaw failed: %w", callErr_)
		}
	}
	return nil
}

func (w *procWrapper) Getpid(ctx context.Context) (int32, error) {
	var resVal_ int32
	{
		callErr_ := w.fnGetpid.CallRaw(ctx, w.callerInst,
			func(caller, callee *host.CallContext, stack []uint64) {
			},
			func(caller, callee *host.CallContext, stack []uint64) {
				resVal_ = int32(int64(stack[0]))
			},
		)
		if callErr_ != nil {
			return 0, fmt.Errorf("wacogo/witgen: CallRaw failed: %w", callErr_)
		}
	}
	return resVal_, nil
}

func (w *procWrapper) Getppid(ctx context.Context) (int32, error) {
	var resVal_ int32
	{
		callErr_ := w.fnGetppid.CallRaw(ctx, w.callerInst,
			func(caller, callee *host.CallContext, stack []uint64) {
			},
			func(caller, callee *host.CallContext, stack []uint64) {
				resVal_ = int32(int64(stack[0]))
			},
		)
		if callErr_ != nil {
			return 0, fmt.Errorf("wacogo/witgen: CallRaw failed: %w", callErr_)
		}
	}
	return resVal_, nil
}

func (w *procWrapper) Kill(ctx context.Context, pid int32, sig int32) (int32, error) {
	var resVal_ int32
	arg0_ := pid
	arg1_ := sig
	{
		callErr_ := w.fnKill.CallRaw(ctx, w.callerInst,
			func(caller, callee *host.CallContext, stack []uint64) {
				stack[0] = uint64(uint32(arg0_))
				stack[1] = uint64(uint32(arg1_))
			},
			func(caller, callee *host.CallContext, stack []uint64) {
				resVal_ = int32(int64(stack[0]))
			},
		)
		if callErr_ != nil {
			return 0, fmt.Errorf("wacogo/witgen: CallRaw failed: %w", callErr_)
		}
	}
	return resVal_, nil
}

func (w *procWrapper) Spawn(ctx context.Context, argvAddr uint32, envpAddr uint32, cwdAddr uint32) (int32, error) {
	var resVal_ int32
	arg0_ := argvAddr
	arg1_ := envpAddr
	arg2_ := cwdAddr
	{
		callErr_ := w.fnSpawn.CallRaw(ctx, w.callerInst,
			func(caller, callee *host.CallContext, stack []uint64) {
				stack[0] = uint64(arg0_)
				stack[1] = uint64(arg1_)
				stack[2] = uint64(arg2_)
			},
			func(caller, callee *host.CallContext, stack []uint64) {
				resVal_ = int32(int64(stack[0]))
			},
		)
		if callErr_ != nil {
			return 0, fmt.Errorf("wacogo/witgen: CallRaw failed: %w", callErr_)
		}
	}
	return resVal_, nil
}

func (w *procWrapper) Waitpid(ctx context.Context, pid int32, statusAddr uint32, options int32) (int32, error) {
	var resVal_ int32
	arg0_ := pid
	arg1_ := statusAddr
	arg2_ := options
	{
		callErr_ := w.fnWaitpid.CallRaw(ctx, w.callerInst,
			func(caller, callee *host.CallContext, stack []uint64) {
				stack[0] = uint64(uint32(arg0_))
				stack[1] = uint64(arg1_)
				stack[2] = uint64(uint32(arg2_))
			},
			func(caller, callee *host.CallContext, stack []uint64) {
				resVal_ = int32(int64(stack[0]))
			},
		)
		if callErr_ != nil {
			return 0, fmt.Errorf("wacogo/witgen: CallRaw failed: %w", callErr_)
		}
	}
	return resVal_, nil
}
